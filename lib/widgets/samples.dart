import 'package:flutter/material.dart';

class Samples extends StatelessWidget {
  const Samples({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Amostras');
  }
}
